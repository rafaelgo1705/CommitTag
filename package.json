{
  "name": "commit-tag",
  "version": "1.0.20",
  "main": "index.js",
  "repository": "git@github.com:rafaelgo1705/commit-tag.git",
  "author": "Rafael Gomes",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "commit-tag": "git add . && yarn version --patch && git push --follow-tags && git push --follow-tags"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^5.1.1"
  },
  "devDependencies": {
    "prettier": "^3.0.3",
    "typescript": "^5.2.2"
  }
}
